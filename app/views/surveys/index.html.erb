<div class="WorkArea">

<h1>Listing surveys</h1>

<table>
  <thead>
    <tr>
      <th>Name</th>
      <th>Who survey code</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @surveys.each do |survey| %>
      <tr>
        <td><%= survey.name %></td>
        <td><%= survey.who_survey_code %></td>
        <td><%= link_to 'Show', survey %></td>
        <td><%= link_to 'Edit', edit_survey_path(survey) %></td>
        <td><%= link_to 'Destroy', survey, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= button_to 'New Survey', new_survey_path, :method => "get" , :class => "btn btn-primary" %>
</div>
