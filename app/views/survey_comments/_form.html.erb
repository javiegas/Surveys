<%= form_for(@survey_comment) do |f| %>
  <% if @survey_comment.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@survey_comment.errors.count, "error") %> prohibited this survey_comment from being saved:</h2>

      <ul>
      <% @survey_comment.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :comment %><br>
    <%= f.text_field :comment %>
  </div>
  <div class="field">
    <%= f.label :survey_id %><br>
    <%= f.number_field :survey_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
